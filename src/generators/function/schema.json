{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "FunctionApp",
  "title": "Initialize a function for an NX application",
  "type": "object",
  "properties": {
    "projectName": {
      "type": "string",
      "alias": "project",
      "description": "The name of the project to initialize.",
      "x-dropdown": "projects"
    },
    "functionName": {
      "type": "string",
      "description": "The name of the function",
      "alias": "function",
      "x-prompt": "What is the name of the azure function?"
    }
  },
  "required": ["projectName", "functionName"]
}
