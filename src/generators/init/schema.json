{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "FunctionApp",
  "title": "Initialize a function app for an NX application",
  "type": "object",
  "properties": {
    "projectName": {
      "type": "string",
      "alias": "project",
      "description": "The name of the project to initialize.",
      "x-dropdown": "projects"
    }
  },
  "required": ["projectName"]
}
